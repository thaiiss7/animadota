import { Header } from "../components/Header";
import { SideBar } from "../components/SideBar";

export const LikePage = () => {
    return(
        <>
            <main>
                <Header/>
                <SideBar/>
            </main>
        </>
    );
}