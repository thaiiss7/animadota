export const EditProfile = ({ aberto, fechar }) => {
    return (
        <>
            <main className="w-full h-screen fixed inset-0 bg-black/50 flex items-center justify-center">
                <div>
                    
                </div>
            </main>
        </>
    );
}