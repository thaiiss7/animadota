services:
  api:
    build:
      context: .
      dockerfile: Back
    
    ports:
      - 4000:8080

    env_file:
      - .env

  db:
    image: mcr.microsoft.com/mssql/server:2022-latest

    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "senha134hfhs99!"

    volumes:
      - Animadota:/var/opt/mssql

volumes:
  Animadota: